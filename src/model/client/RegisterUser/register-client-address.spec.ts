import { RegisterClientAddress } from './register-client-address';

describe('RegisterClientAddress', () => {
  it('should create an instance', () => {
    expect(new RegisterClientAddress()).toBeTruthy();
  });
});
