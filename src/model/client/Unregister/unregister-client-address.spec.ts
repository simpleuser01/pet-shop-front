import { UnregisterClientAddress } from './unregister-client-address';

describe('UnregisterClientAddress', () => {
  it('should create an instance', () => {
    expect(new UnregisterClientAddress()).toBeTruthy();
  });
});
